<div>
	<span class="page_title">
		ジャンルのリスト
	</span>
	
	&nbsp;&nbsp;(<%= link_to 'New Genre', new_genre_path %>)</div>

<br />

<table>
  <tr>
  	<th>Id</th>
    <th>Name</th>
  </tr>

<% @genres.each do |genre| %>
  <tr>
  	<td><%= genre.id %></td>
    <td><%= link_to genre.name, genre %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Genre', new_genre_path %>
